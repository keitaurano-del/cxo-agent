// 仕事ページ（/work）「ピボット」タブ用の説明テキスト（MC-260 / MC-261）。
//
// MC-261 で、1枚の長い Markdown を「図解の前後に置く短い手順テキスト」に分割した。
// 図は workPivotDiagrams.tsx の SVG コンポーネントとして実装し、Work.tsx の WorkPivotTab で
// テキストブロック（ChatMarkdown）と図を交互に並べる。各セクションは下記の named export を使う。
// 練習用 Excel はドキュメント →「大手町」フォルダの「Excelピボット練習_ECLデータ.xlsx」。

// イントロ（ピボットとは／図1 before→after の前に置く）。
export const PIVOT_INTRO_MD = `## ピボットテーブルとは

大量の表データを、ドラッグ操作だけで「集計表（クロス集計）」に変えられる Excel の機能です。行・列・値に項目を置くだけで、合計・件数・平均などを瞬時に出せます。SUMIFS などの数式を書かずに集計できるのが最大の利点で、ECL の状況把握（支店別・区分別・ステージ別の集計など）に直結します。

下の図は、縦に長い生データ（80 行）が、ドラッグ操作だけで「行＝債務者区分 × 列＝ステージ（S1／S2／S3）、値＝ECL 合計」のクロス集計表に変わる様子です。引当（ECL）が S3（破綻懸念先）に偏っていることが、一目で分かります。`;

// 作る手順の導入（図2 リボン操作の前）。
export const PIVOT_STEPS_INTRO_MD = `## 作る手順

ここからは、上のクロス集計表を実際に作る手順を、4 ステップの図で順番に見ていきます。練習データのカラムは「支店／業種／商品種別／債務者区分／ステージ／EAD／PD／LGD／ECL」です。`;

// 手順1〜2 のテキスト（図2 リボン操作の後・図3 フィールドペインの前）。
export const PIVOT_STEP_INSERT_MD = `表内のセルを 1 つクリックしてから［挿入］→［ピボットテーブル］を選び、配置先に「新規ワークシート」を選んで OK を押します。すると、右側に空のピボットと「フィールド」一覧が表示されます。`;

// 手順3 のテキスト（図3 フィールドペインの後・図4 値設定の前）。
export const PIVOT_STEP_FIELDS_MD = `右側の「フィールド」一覧から、項目を 4 つのボックス（フィルター／列／行／値）へドラッグします。今回の例では、債務者区分を「行」、ステージを「列」、ECL を「値」へドラッグすると、上で見たクロス集計表ができます。`;

// 集計方法の変更の導入（図4 値設定の前）。
export const PIVOT_AGG_INTRO_MD = `## 集計方法を変える

「値」に置いた項目は、初期状態では「合計」で集計されます。合計以外（個数・平均など）にしたいときは、値を右クリック →［値フィールドの設定］で切り替えます。`;

// 完成イメージ＋使いどころの導入（図5 完成イメージの前）。
export const PIVOT_RESULT_INTRO_MD = `## 完成イメージと使いどころ

出来上がったクロス集計には、スライサー（支店・ステージをボタンで切り替える絞り込み）を添えると、点検がさらに速くなります。［ピボットテーブル分析］→［スライサーの挿入］で追加できます。`;

// よく使う操作（図のあとにまとめて置く）。
export const PIVOT_TIPS_MD = `## よく使う操作

- 表示形式：値を右クリック →［表示形式］で、桁区切り（#,##0）や％にすると一気に見やすくなります。
- グループ化：日付の項目を行に置き、右クリック →［グループ化］で「月」「四半期」単位にまとめられます。計上月別の推移を見るときに使います。
- 並べ替え：値の列で右クリック →［並べ替え］で、金額の大きい順などに並べられます。
- 更新：元データを変えたら、ピボット上で右クリック →［更新］。自動では更新されない点に注意してください。

## この案件での使いどころ

- 支店別・業種別・債務者区分別・ステージ別の EAD／ECL 集計。
- 計上月別の引当（ECL）の推移。
- クロス集計（債務者区分 × ステージ）で、どこに引当が偏っているかの把握。
- ステージ 2・3 だけに絞った支店別の状況点検。

いずれも、ピボットなら数分で表にできます。ECL の点検・報告の下ごしらえに有効です。`;

// 練習用ファイル案内（末尾）。
export const PIVOT_PRACTICE_MD = `## 練習用ファイル

実際に手を動かして練習できる Excel を用意しました。

- 場所：ドキュメント →「大手町」フォルダ →「Excelピボット練習_ECLデータ.xlsx」
- 中身：
  - ピボットの使い方（手順のまとめ）
  - 練習データ（銀行／ECL を模した 80 行の表。支店・業種・商品種別・債務者区分・ステージ・EAD・PD・LGD・ECL）
  - 練習問題（支店別集計やクロス集計など 8 問）
  - 解答（集計例）（SUMIFS 等で集計した答え。ピボットの結果と見比べて答え合わせ）

まず「練習データ」のセルを 1 つ選んで［挿入］→［ピボットテーブル］から、Q1 の「支店別の EAD 合計・ECL 合計」を作ってみてください。`;

// ════════════════════════════════════════════════════════════════════════════
// 応用・実務（コンサル水準）セクション。
// 基礎（挿入→配置→集計→完成→スライサー）の「その先」を、実務で差がつく7本柱でまとめる。
// ECL/銀行の例で具体化しつつ、汎用的に使える形。手順性は薄いので図は step バッジ無し・title のみ。
// ════════════════════════════════════════════════════════════════════════════

// 応用セクションの導入（PIVOT_PRO_* の先頭）。
export const PIVOT_PRO_INTRO_MD = `## 応用・実務（コンサル水準）

ここからは、基礎の「その先」です。コンサルティングファームの実務でピボットを「報告に出せる成果物」まで仕上げるためのテクニックを、差がつく順に 7 つにまとめます。いずれも ECL の例で具体化していますが、考え方はそのまま他の集計にも使えます。

- データ準備（テーブル化＋Power Query）
- データモデル＆DAX メジャー
- 「値の表示形式」フルカタログ
- 差異の要因分解（ブリッジ）
- Top-N＋「その他」集約・寄与度
- 成果物連携（デック化）
- 監査・QA・落とし穴＋運用規律

まずは「集計の前」、データ準備から始めます。`;

// 1. データ準備＝テーブル化＋Power Query(ETL)。
export const PIVOT_PRO_POWERQUERY_MD = `### 1. データ準備：テーブル化＋Power Query（ETL）

ピボットの精度は、元データの形でほぼ決まります。集計を始める前に、データを整える工程（ETL）を挟むのがプロの作法です。

- テーブル化：元データのセルを 1 つ選んで Ctrl+T で「テーブル」に変換します。行を足すと範囲が自動で広がり、ピボットの参照元が常に最新になります（単一の真実）。名前付きで数式・ピボットから安定参照できます。
- tidy data（整然データ）原則：1 セルに 1 つの値、結合セルは使わない、横持ち（月が列に広がる形）ではなく縦持ち（1 行 1 観測）にそろえます。これがピボットの大前提です。
- Power Query（ETL）：［データ］→［データの取得］から、複数ファイル・複数シートを取り込み、結合（append：縦に積む）・マージ（merge：キーで横に結合）・列のアンピボット（横持ちを縦持ちに変換）でピボット可能な形に整えます。手作業のコピペを置き換え、更新ボタン 1 つで再現できる前処理になります。

下の図は、複数ソース → クレンジング → アンピボット → 1 つのテーブル → ピボット、という ETL フローです。`;

// 2. データモデル＆DAXメジャー。
export const PIVOT_PRO_DATAMODEL_MD = `### 2. データモデル＆DAX メジャー

複数のテーブルを 1 枚に結合せず、「データモデル」上でリレーション（関係）として結ぶと、明細（取引）とマスタ（支店・債務者）を分けたまま集計できます。

- リレーション：明細テーブルとマスタテーブルを共通キー（支店コード等）でつなぎます。VLOOKUP で 1 枚に貼り合わせる必要がなくなります。
- 「平均の平均」を避ける加重平均：PD・LGD のような比率は、単純平均すると誤ります。DAX メジャーで \`SUMX\` を使い、EAD で加重した平均（= Σ(LGD×EAD) ÷ Σ EAD）として明示計算します。比率（LGD 率など）も「分子の合計 ÷ 分母の合計」をメジャーで定義します。
- タイムインテリジェンス：前年同期（PY）・前月（MoM）・前年比（YoY）・累計（YTD）・年平均成長率（CAGR）や run-rate を、\`CALCULATE\` と \`DATESYTD\` 等のメジャーで計算します。

下の図は、2〜3 テーブルのリレーションと、加重平均・YoY メジャーの概念図です。`;

// 3. 「値の表示形式」フルカタログ。
export const PIVOT_PRO_SHOWVALUES_MD = `### 3. 「値の表示形式」フルカタログ

同じ数値でも、「見せ方」を変えると別の示唆が出ます。値を右クリック →［値の表示形式］で、計算済みの値を一切作らずに切り替えられます。

- 比率：総計に対する比率／親行集計に対する比率／親列集計に対する比率。構成比（どの区分が何％か）を出します。
- 順位：昇順・降順の順位。支店ランキングなどに。
- 累計：累計／累計構成比（パレート分析の土台）。
- 比較：基準値との差／基準値との差の比率（%）、指数。

同じクロス集計を「金額 → 構成比 → 順位 → 累計」と切り替えて見るのがコツです。下の図は、同一のクロス集計を 4 つの表示形式で並べたスモールマルチプルです。`;

// 4. 差異の要因分解（ブリッジ/ウォーターフォール）。
export const PIVOT_PRO_BRIDGE_MD = `### 4. 差異の要因分解（ブリッジ／ウォーターフォール）

「予算と実績がなぜ違うのか」を、ピボットで要因に切り分けて説明します。報告で最も問われる「差の中身」を可視化する手法です。

- 価格 × 数量 × ミックス分解：金額差を、単価の変化（価格）・件数の変化（数量）・構成の変化（ミックス）に分けます。ECL なら、PD/LGD の変化・残高の変化・ポートフォリオ構成の変化に対応します。
- ブリッジ（ウォーターフォール）：起点（予算）から各要因の増減を積み上げ、終点（実績）へつなぐ橋を描きます。増減の符号と大きさが一目で伝わります。

下の図は、予算 → 各要因の増減バー → 実績、というウォーターフォールです。`;

// 5. Top-N＋「その他」集約・寄与度。
export const PIVOT_PRO_TOPN_MD = `### 5. Top-N＋「その他」集約・寄与度

報告の表は「全件」より「上位＋その他」の方が伝わります。

- Top-N：行ラベルの［値フィルター］→［上位 10］等で、上位 N 件だけを表示します。
- 「その他」集約：残りをまとめて「その他」バケットに畳み、総計を保ったまま見やすくします。
- 寄与度：前年差（や予算差）の合計に対し、各項目がどれだけ効いたかの貢献度を出します。「全体が増えた／減った主因はどこか」を定量で示せます。

下の図は、上位 N のバーと「その他」バケット、そして各項目の寄与度を並べたものです。`;

// 6. 成果物連携（デック化）。
export const PIVOT_PRO_DECKLINK_MD = `### 6. 成果物連携（デック化）

ピボットは「分析の場」、報告は「別のきれいな表・スライド」に分けるのが定石です。両者を安定してつなぎます。

- GETPIVOTDATA：ダッシュボードやサマリ表から、ピボットの特定セルを安定参照します。ピボットの形が変わっても、項目名で値を拾えます。動的タイトル（="〇〇支店 ECL サマリ "&… ）と組み合わせます。
- レポート接続（スライサー連動）：1 つのスライサーを［レポート接続］で複数のピボット／ピボットグラフに紐づけ、同時に絞り込みます。タイムライン（期間スライダー）も同様に連動できます。
- レイアウト：表形式（Tabular）・項目ラベルの繰り返し・小計の制御で、「報告に出せる表」に整えます。ピボットグラフは PowerPoint にリンク貼り付けし、更新を反映できます。

下の図は、1 つのスライサーが複数のピボット／グラフを同時に絞るレポート接続と、GETPIVOTDATA 参照のイメージです。`;

// 7. 監査・QA・落とし穴＋運用規律。
export const PIVOT_PRO_QA_MD = `### 7. 監査・QA・落とし穴＋運用規律

最後に、ピボットを「信頼できる成果物」にするためのチェックです。

- 落とし穴：平均の平均（比率の単純平均）／二重計上（マージのキー重複）／フィルタの掛け忘れ／古いキャッシュ（更新忘れ）／ピボット結果を手修正で上書き。いずれも数字を静かに狂わせます。
- 突合：総計が元データの合計と一致するか、件数・金額の両面で確認します。
- 更新規律：［データソースの設定］で「ファイルを開くときに更新」をオンにし、編集後は必ず［更新］を押します。データ範囲がテーブル名であることも確認します。
- パフォーマンス：使わないフィールドは外し、ピボットキャッシュの肥大を抑えます。

下の図は、QA チェックリストと、「平均の平均」の誤り例を正誤で対比したものです。`;
