# MC-363 — ビジネスモデル図鑑

- **優先度:** 高
- **担当:** Son（subagent ディスパッチ）
- **インデックス行の正本:** docs/TASK_TRACKER.md（ステータスはそちらの行が正）
- **備考/依存:** 開発（/dev）ページに常設。MC-361（モックアップ図解標準化）の図解方針と整合させる

## 依頼原文（2026-08-02 15:27 Keita）

「よのなかのビジネスをビジネスモデルの図にしてほしいんだよね。開発のところにまとめてほしい。優れているビジネスモデルを図解してわかりやすくしてほしい。」

## スコープ

- 世の中の優れたビジネスモデル（10〜15種）を1モデル=1図解のカタログとして開発ページ配下に常設
- 各モデル: SVG図解（登場プレイヤー・価値の流れ・カネの流れ）＋「なぜ優れているか」（囲い込み/ネットワーク効果/スイッチングコスト等）＋代表企業例
- 実装: 新規 `web/src/views/BusinessModels.tsx`（自己完結・フロントのみ）＋ App.tsx ルート結線＋ Development.tsx から導線
- 制約: ソラWIP（BottomNav.tsx / icons.tsx / devMockupRouter.ts）に触れない。アイコンは既存を流用。dist ビルドのみ＝backend再起動不要

## Definition of Done（DONE にする前に埋める）

- 成果物の場所: `web/src/views/BusinessModels.tsx`（新規・自己完結）＋ `web/src/App.tsx`（ルート `/dev/business-models` 追加）＋ `web/src/views/Development.tsx`（左ペイン上部に「📖 ビジネスモデル図鑑」導線カード）
- 検証方法と結果: `cd web && npm run build`（tsc -b && vite build）**PASS**（TS エラー 0・BusinessModels チャンク生成）。本番実測（2026-08-02 16:2x・curl+Cookie 認証）: `/dev/business-models` **200**、`assets/BusinessModels-CRmjmqi_.js` **200**（27,889 bytes・本文に日本語コンテンツ確認）、本番 index.html のバンドルハッシュ `index-Dbk6rUXe.js` がローカル dist と一致＝新ビルド配信中を実証
- 本番反映有無: あり（dist 静的配信のみ・backend 再起動不要）。ソラWIP（BottomNav.tsx / icons.tsx / devMockupRouter.ts）非接触

## 経緯ログ

**【2026-08-02 16:2x Son・実装完了】** 15時台に「subagent実装中」と起票されたがセッション消滅で成果物ゼロと判明→毎時ループ(MC-354)で再ディスパッチ。subagent が12モデル（サブスク/フリーミアム/両面市場/替え刃/広告/マーケットプレイス手数料/SPA/ライセンス/D2C/シェアリング/データ活用/フランチャイズ）を1モデル=1SVG図解（プレイヤー箱・価値の流れ=実線青・カネの流れ=破線アンバー・凡例付）＋「なぜ優れているか」バッジ＋代表企業チップで実装。冒頭にアンカーTOC。既存 workPivotDiagrams.tsx の SVG イディオム（--mc-* トークン・viewBox レスポンシブ・aria-label）踏襲。ビルド→本番配信を上記のとおり実測済。残: 実画面での見た目確認は Keita レビュー待ち（REVIEW へ）。
- 2026-08-03 21:12 Keita 決裁フローで OK → DONE（決裁ID dec-a35b1f09）
