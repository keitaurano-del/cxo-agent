# MC-371 — 生成の迷いファイル書き出し対策

- **優先度:** 中
- **担当:** Son
- **インデックス行の正本:** docs/TASK_TRACKER.md（ステータスはそちらの行が正）
- **備考/依存:** MC-361 の5案生成中に発見。対象 devMockupRouter.ts（コード段の claude CLI 呼び出し）

## Definition of Done（MC-358 層3）

- 成果物の場所: server/src/devMockupRouter.ts（commit ed8d1d3）= ①生成系CLI呼び出し6箇所をツール無効起動（RunClaudeOpts.noTools → `--tools ''`。cwd/system prompt/拡張思考は不変） ②HTML_RULESに「ファイルへの書き出し禁止・応答本文のみ」を明記 ③保存前バリデーション=`</html>`完結を要求（generateHtmlWithRetry＝リトライ対象化・refineパス＝良品を途中切れで上書きしない）
- 検証方法と結果: tsc --noEmit green。restart反映後、実生成1件（ポモドーロタイマー・wireframe省略）で検証: job done・保存HTML 17KBが`</html>`完結・リポジトリへの迷いファイル新規発生ゼロ。過去迷いファイル5件は対応モックアップの健全性を個別確認（4件OK・airshareのみ同型破損→完全版からupsert修復）の上 `_archive/mc371-strays-20260809/` へ退避
- 本番反映有無: あり（2026-08-09 14:40 mission-control restart・health ok）

**【2026-08-09 15:30 Son・毎時ループ】完了。**

## 経緯ログ

- 2026-08-09: MC-361 の5案一括生成（6ジョブ）中に2件で発生。コード段のエージェントが完成HTMLを stdout で返さず、リポジトリ直下 `cxo-agent/tategu-monogatari.html` へ Write ツールで書き出し（2件とも同名・内容は各ジョブのアプリ）、stdout には途中で切れたHTML＋説明文（「完成したのじゃ。…に出力済みじゃ」）が返って**破損したまま履歴保存**された。過去の迷いファイル（airshare-prototype.html / kurabito-match.html / server/hatake-meguri.html / mimamori-pet.html / tategu-monogatari.html、8/2〜8/8）も同根とみられる。
- 当日の対処: 完全版ファイルから `POST /api/dev/mockups` で upsert 修復（案2=114e4133・案4=c8c4c5d3）。迷いファイル2件は /tmp へ退避（mc361-stray-*.html）。
- 恒久対策の候補:
  1. コード段の claude CLI 呼び出しをツール無効（idea 生成と同じ fast オプション相当）で起動し、ファイル書き出し自体を不可能にする
  2. プロンプトに「ファイルへ書き出さず、完成HTMLを応答本文のみで返すこと」を明記
  3. 保存前バリデーション: `</html>` を含まない html は保存せずリトライ（または error 扱いにして partial を提示）
- 過去の迷いファイル5件の掃除（対応するモックアップが健全か確認してから退避）も本タスクに含む。
