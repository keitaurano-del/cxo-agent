# MC-372: DL履歴の可視化

- **依頼:** 2026-08-09 22:47 Keita「ClipItNowで何の動画がダウンロードされたかみれるようにしてほしい」
- **担当:** Son
- **ステータス:** DONE（2026-08-09 23:05）

## 実装

- **video-dl** `server.py` commit 4cba9e8
  - `/api/dlhistory?limit=N`（既定50・最大500）: `downloads.jsonl` を新しい順に返す（ts/url/title/thumbnail/host、サンプルURL除外）。
  - **秘匿:** cloudflared 経由（`CF-Connecting-IP` ヘッダあり）は 404。個別DL履歴は公開せず、ローカル直叩き（Apollo backend）のみ。
- **cxo-agent**
  - `server/src/revenueRouter.ts`: `/api/dlhistory?limit=30` を並列取得し `clipitnow.recentDownloads` として summary に同梱。
  - `web/src/views/Revenue.tsx`: 収益タブの ClipItNow セクション直下に「最近ダウンロードされた動画（新しい順・最大30件）」一覧（サムネ・タイトル→元URLリンク・サイト名・M/D HH:mm）。

## DoD

1. **成果物の場所:** video-dl `server.py`（commit 4cba9e8）／cxo-agent `revenueRouter.ts`・`Revenue.tsx`（本コミット）。表示先= apollomansion.com 収益タブ最下部。
2. **検証方法と結果:**
   - `curl localhost:4319/api/dlhistory?limit=3` → 200・履歴返却（新しい順）。
   - `curl -H "CF-Connecting-IP: 1.2.3.4" localhost:4319/api/dlhistory` → 404（外部秘匿）。
   - 外形 `https://clipitnow.net/api/dlhistory` → 404 ／ `/api/stats` → 200（既存無影響）。
   - `/api/revenue/summary` → `recentDownloads` 8件同梱。tsc/vite build green。
   - OpenClawブラウザで apollomansion.com/revenue 実画面確認 → 一覧描画OK（スクショ media/browser/a7a1a847）。
3. **本番反映有無:** あり。video-dl.service・mission-control.service とも再起動済・実画面確認済。

## 備考

- サムネは配信元CDNの署名付きURLのため期限切れで表示されない場合あり（onErrorで非表示にフォールバック）。
- 履歴の元データは従来から `downloads.jsonl` に記録済（ランキング用）。今回の追加は読み出し口と画面のみ。
