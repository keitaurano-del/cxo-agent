// 仕事ページ（/work）「課題管理費の集計」ピボット図解タブ用の説明テキスト。
//
// 既存の workPivotGuide.ts（ECL 残高のクロス集計が例）と同じ作法で、課題管理費（プロジェクトの費用）の
// 集計に特化した実務ガイドを named export で用意する。1 枚の長い Markdown ではなく「図解の前後に置く
// 短い手順テキスト」に分割し、図は workCostPivotDiagrams.tsx の SVG コンポーネントとして実装する。
// Work.tsx 側で、テキストブロック（ChatMarkdown）と図を交互に並べて配置する想定。
//
// 想定するサンプルデータ（課題管理＝費用一覧）のカラム：
//   課題ID / 案件（サブPJ）/ フェーズ / 費目 / 担当（ベンダー・部署）/ 発生月（YYYY-MM）/ 工数（人日）/ 費用（円）/ ステータス

// イントロ（課題管理費をピボットで集計する意義／図1 before→after の前に置く）。
export const COST_PIVOT_INTRO_MD = `## 課題管理費をピボットで集計する

課題管理表（プロジェクトで発生した費用の一覧）は、行が増えるほど「費目別・月別・ベンダー別にいくら掛かっているのか」が見えにくくなります。ピボットテーブルを使えば、ドラッグ操作だけで「費目 × 発生月」のクロス集計に変えられ、費目別・月別・ベンダー別の費用把握が一目でできます。

さらに、SUMIFS などの数式を書かなくても、［計算の種類］を選ぶだけで構成比（％）や累計を出せるのが大きな利点です。ECL（予想信用損失）システム導入 PMO の費用管理では、フェーズ別・費目別の費用の偏りや、月を追うごとの累計の伸びを把握する下ごしらえに直結します。

下の図は、縦に長い費用一覧が、ドラッグ操作だけで「行＝費目 × 列＝発生月、値＝費用合計」のクロス集計表に変わる様子です。外注費が特定のフェーズ・月に偏っていることが、数式なしで一目で分かります。`;

// 作る手順の導入＋サンプル列の説明（図2 フィールドペインの前）。
export const COST_PIVOT_STEPS_INTRO_MD = `## サンプルの列と作る手順

ここからは、上のクロス集計表を実際に作る手順を、図で順番に見ていきます。サンプルとして次の列構成の費用一覧を使います（実データに合わせて読み替え可）。

- 課題ID … 一意の管理番号
- 案件（サブPJ）… ECL 計算基盤・データ移行 など
- フェーズ … 要件定義 / 設計 / 開発 / テスト など
- 費目 … 人件費 / 外注費 / ライセンス / 経費 など
- 担当（ベンダー・部署）… 発注先や担当部署
- 発生月 … YYYY-MM 形式（例：2026-06）
- 工数（人日）… 投入した人日数
- 費用（円）… 発生した金額
- ステータス … 計上済 / 申請中 など

まず表内のセルを 1 つクリックしてから［挿入］→［ピボットテーブル］を選び、配置先に「新規ワークシート」を選んで OK を押します。すると右側に空のピボットと「フィールド」一覧が表示されます。`;

// フィールド配置の説明（図2 フィールドペインの後）。
export const COST_PIVOT_FIELDS_MD = `右側の「フィールド」一覧から、項目を 4 つのボックス（フィルター／列／行／値）へドラッグします。今回の例では、費目を「行」、発生月を「列」、費用を「値」へドラッグすると、上で見たクロス集計表ができます。

「値」には費用だけでなく、工数（人日）や件数（課題の数）も追加で置けます。費用・工数・件数を並べると、「金額は大きいが工数は少ない＝外注やライセンス中心」といった費目の性格まで読み取れます。`;

// 集計方法・計算の種類の導入（図3 計算の種類の前）。
export const COST_PIVOT_CALC_INTRO_MD = `## 集計方法と「計算の種類」

「値」に置いた費用は、初期状態では「合計」で集計されます。費用集計では、合計に加えて次の 2 つが特に役立ちます。値を右クリック →［値フィールドの設定］→［計算の種類］から選びます。

- 列集計に対する比率：各セルを列の総計で割った構成比（％）。「この月の費用のうち外注費が何％か」が分かります。
- 累計：月を追うごとの費用の積み上げ。「年度内の累計支出が予算に対してどこまで来たか」を追えます。

合計（実額）と構成比（％）・累計を並べて見ると、費用の規模・割合・推移を 1 つの表で把握できます。`;

// グループ化・表示形式・並べ替えの導入（図4 グループ化の前）。
export const COST_PIVOT_GROUPING_INTRO_MD = `## 月→四半期のグループ化・表示形式・並べ替え

発生月（YYYY-MM）を行や列に置くと、月単位で細かく並びます。発生月のセルを右クリック →［グループ化］で「四半期」「年」単位にまとめられます。月次では細かすぎる報告では、四半期単位に畳むと費用の推移が読みやすくなります。`;

// 表示形式・並べ替えの補足（図4 グループ化の後）。
export const COST_PIVOT_FORMAT_MD = `金額は、値を右クリック →［表示形式］で桁区切りや単位を整えると一気に見やすくなります。

- 円単位：\`¥#,##0\`（例：¥1,234,567）
- 千円単位：\`#,##0,\` の末尾カンマで 1/1,000 表示（千円）
- 百万円単位：\`#,##0,,\` の末尾カンマ 2 つで 1/1,000,000 表示（百万円）

また、費用の列で右クリック →［並べ替え］→「降順」にすると、費用上位のベンダーや費目を抽出できます。「どのベンダーに費用が集中しているか」を素早く洗い出すときに使います。`;

// 完成イメージ＋スライサーの導入（図5 完成イメージの前）。
export const COST_PIVOT_RESULT_INTRO_MD = `## 完成イメージと使いどころ

出来上がったクロス集計に、構成比（％）の列とスライサー（担当・フェーズをボタンで切り替える絞り込み）を添えると、費用点検がさらに速くなります。スライサーは［ピボットテーブル分析］→［スライサーの挿入］で追加できます。`;

// よく使う操作＋この案件での使いどころ（図5 のあとにまとめて置く）。
export const COST_PIVOT_TIPS_MD = `## よく使う操作

- 計算の種類：費用に「列集計に対する比率」で構成比（％）、「累計」で月次の積み上げを出せます（費用集計で特に有用です）。
- グループ化：発生月を行や列に置き、右クリック →［グループ化］で「四半期」「年」単位にまとめます。
- 表示形式：金額は \`¥#,##0\` や千円・百万円単位にすると桁が揃って読みやすくなります。
- 並べ替え：費用の列で右クリック →［並べ替え］→「降順」で、費用上位のベンダー・費目を抽出します。
- 更新：元の費用一覧を変えたら、ピボット上で右クリック →［更新］。自動では反映されない点に注意してください。

## この案件での使いどころ

- 費目別（人件費・外注費・ライセンス・経費）× 発生月の費用集計。
- フェーズ別（要件定義・設計・開発・テスト）の費用配分と構成比の把握。
- ベンダー・部署別の費用集計と、降順並べ替えによる費用上位の抽出。
- 累計列による、年度内の累計支出と予算消化ペースの確認。
- 費用と工数（人日）を並べた、費目の性格（金額中心か工数中心か）の把握。

いずれも、ピボットなら数分で表にできます。ECL システム導入 PMO の費用報告・予実管理の下ごしらえに有効です。`;

// 練習用ファイル案内（末尾）。
export const COST_PIVOT_PRACTICE_MD = `## 練習用ファイル

実際に手を動かして練習できる Excel を用意する想定です。

- 場所：ドキュメント →「大手町」フォルダ →「Excelピボット練習_課題管理費データ.xlsx」（※準備中）
- 中身（予定）：
  - ピボットの使い方（費用集計の手順まとめ）
  - 練習データ（課題管理＝費用を模した一覧。課題ID・案件・フェーズ・費目・担当・発生月・工数・費用・ステータス）
  - 練習問題（費目別集計、費目 × 月のクロス集計、構成比・累計、ベンダー別の降順抽出 など）
  - 解答（集計例）（SUMIFS 等で集計した答え。ピボットの結果と見比べて答え合わせ）

準備でき次第、ここに正式なファイルを差し替えます。まずはお手元の課題管理表のセルを 1 つ選んで［挿入］→［ピボットテーブル］から、「費目別の費用合計」を作ってみてください。`;

// ════════════════════════════════════════════════════════════════════════════
// より高度な分析セクション（基礎の集計に慣れた後に足す 5 つのテクニック）。
// 各テーマは「短い説明 MD ＋ 図 1 枚」を対にして配置する想定。
// ════════════════════════════════════════════════════════════════════════════

// 高度分析セクションの導入（このセクション全体の前に置く）。
export const COST_PIVOT_ADV_INTRO_MD = `## より高度な分析

ここまでの「費目 × 月のクロス集計・構成比・累計・グループ化・スライサー」に慣れたら、次の 5 つを足すと、費用集計が「実績の把握」から「予実管理・異常検知」まで広がります。いずれも生データに列を足したり数式を書いたりせず、ピボットの機能だけで実現できます。

1. 計算フィールド：単価や予実差異を、ピボットの中で算出する。
2. 値の表示形式：前月比・累計・構成比を、計算の種類を選ぶだけで出す。
3. ABC 分析（パレート）：費用を降順に並べ、累計構成比で A／B／C に区分する。
4. 条件付き書式：クロス集計セルをヒートマップにして、突出した月を一目で見つける。
5. 予実対比：実績ピボットと予算表を結合し、差異・消化率を 1 表にまとめる。

ECL システム導入 PMO の費用管理では、これらを使うと「どのベンダーに費用が集中しているか」「予算をどこまで消化したか」「想定外に膨らんだ月はどこか」を、報告のたびに手計算せずに追えるようになります。`;

// 1. 計算フィールド（図の前）。
export const COST_PIVOT_ADV_CALCFIELD_MD = `### 1. 計算フィールドで単価・予実差異を算出する

費用や工数を足し算するだけでなく、それらを組み合わせた指標（単価・差異・消化率）をピボットの中で計算できます。生データに新しい列を足す必要はありません。

［ピボットテーブル分析］→［フィールド/アイテム/セット］→［集計フィールド］を開き、名前と数式を入れると、値エリアに新しい列として現れます。

- 単価＝\`=費用/工数\`（1 人日あたりの費用。ベンダー間のコスト比較に使えます）
- 予実差異＝\`=実績-予算\`（プラスなら予算超過。費目別・月別に出せます）
- 消化率＝\`=実績/予算\`（表示形式をパーセントにすると消化率%になります）

数式に使えるのは「値に置けるフィールド名」です。費用・工数のような元データの列を組み合わせて、新しい指標をその場で作れます。`;

// 2. 値の表示形式の応用（図の前）。
export const COST_PIVOT_ADV_TRENDFORMAT_MD = `### 2. 値の表示形式でトレンド（前月比・累計）を出す

数式を一切書かずに、「計算の種類」を選ぶだけで月次のトレンドを表示できます。値を右クリック →［値フィールドの設定］→［計算の種類］から選びます。

- 前月比：［基準値に対する比率］を選び、基準フィールドに「発生月」、基準アイテムに「(前の値)」を指定します。各月が前月の何％かが出ます。
- 累計：［累計］を選ぶと、月を追うごとの積み上げになります。年度内の累計支出を追えます。
- 累計構成比：［比率の累計］で、上位から積み上げた構成比（％）になります。後述の ABC 分析の下ごしらえになります。

同じ「費用」を、合計（実額）・前月比（％）・累計の 3 列に並べると、規模・伸び・積み上げを 1 表で読めます。`;

// 3. ABC 分析 / パレート（図の前）。
export const COST_PIVOT_ADV_PARETO_MD = `### 3. ABC 分析（パレート）で重点費目を絞る

費用は一部の費目やベンダーに偏りがちです。費用の大きい順に並べ、累計構成比で 3 段階に区分するのが ABC 分析です。

1. 費目（または担当）を行に、費用を値に置き、費用の降順で並べ替えます。
2. 費用をもう 1 つ値に追加し、計算の種類を［比率の累計］にして累計構成比（％）を出します。
3. 累計構成比でグループ分けします。累計 〜70% を A（重点管理）、〜90% を B、残りを C とするのが目安です。

パレート図（棒＝費用、折れ線＝累計構成比）にすると、「上位わずかな費目で費用の大半を占める」構造が一目で分かります。A ランクの費目・ベンダーに点検を集中すれば、効率よく費用を管理できます。`;

// 4. 条件付き書式で異常検知（図の前）。
export const COST_PIVOT_ADV_HEATMAP_MD = `### 4. 条件付き書式（ヒートマップ）で異常な月を見つける

費目 × 月のクロス集計は、数字を読むだけでは突出した箇所を見逃しがちです。クロス集計セルに条件付き書式を適用すると、金額の大小が色の濃淡で見えるようになります。

値エリアのセルを選び、［ホーム］→［条件付き書式］から選びます。

- カラースケール：金額が大きいセルほど濃く着色されます。費用が突出した月・費目が一目で分かります。
- データバー：セル内に棒が表示され、相対的な大小を横棒で比べられます。

ヒートマップにすると、「外注費が特定の月だけ濃い」といった異常がパッと目に入ります。条件付き書式は表示上の装飾なので、元の数字や集計結果は変わりません。`;

// 5. 予実対比＝データモデルで複数表を結合（図の前）。
export const COST_PIVOT_ADV_BUDGETACTUAL_MD = `### 5. 予実対比：実績と予算表を結合して 1 表にまとめる

実績の費用一覧とは別に、「費目 × 月の予算額」をまとめた予算表があるのが普通です。この 2 つを結合すると、予算・実績・差異・消化率を同じピボットに並べられます。

- パワークエリ／データモデル：実績表と予算表を「費目」「発生月」をキーにリレーション（関係づけ）します。両方を 1 つのピボットのデータ源として扱えるようになります。
- メジャー（DAX）：データモデル上では、\`差異 = SUM(実績[費用]) - SUM(予算[予算額])\` や \`消化率 = DIVIDE(SUM(実績[費用]), SUM(予算[予算額]))\` のような計算式（メジャー）を定義できます。集計フィールドより柔軟で、複数表をまたいだ計算ができます。

これで、費目 × 月のクロス集計に「予算／実績／差異／消化率」が並び、予算超過の費目・月がその場で分かります。ECL 導入 PMO の予実管理レポートを、毎月この 1 表で回せます。`;

// 高度分析の任意の補足（ドリルダウン・連動）。
export const COST_PIVOT_ADV_EXTRAS_MD = `### 補足：ドリルダウンとスライサー連動

- ドリルダウン明細：集計セルの数字をダブルクリックすると、その金額を構成する明細行だけが新しいシートに展開されます。「この月の外注費は、どの課題の費用か」をすぐ確認できます。
- スライサー・タイムラインの連動：1 つのスライサー（担当・フェーズ）やタイムライン（発生月の範囲）を複数のピボット／グラフに接続すると、ボタン操作 1 つで表もパレート図もヒートマップも同時に絞り込めます。費用報告のダッシュボードを 1 画面で操作できます。`;

// ════════════════════════════════════════════════════════════════════════════
// 応用・実務（費用分析・コンサル水準）セクション。
// 既存「より高度な分析」（COST_PIVOT_ADV_*）の後ろに置く、実案件で差がつく 6 本柱。
// 各テーマは「短い説明 MD ＋ 図 1 枚（CostPivotPro*Diagram）」を対にして配置する想定。
// 汎用ピボット側の PIVOT_PRO_*（Power Query / データモデル&DAX / 値の表示形式 /
// ブリッジ / Top-N / デック化 / QA）の「費用版・実践応用」にあたる。重複を避け、
// 費用分析ならではの具体（税区分・按分・計上月・バーンレート・EAC など）に踏み込む。
// ════════════════════════════════════════════════════════════════════════════

// 応用・実務セクションの導入（このセクション全体の前に置く）。
export const COST_PIVOT_PRO_INTRO_MD = `## 応用・実務（費用分析・コンサル水準）

ここまでの集計・高度分析に慣れたら、実案件でコンサルティングファームが費用報告として出すレベルへ引き上げます。鍵は「整形を再現可能にする」「平均・比率を正しく出す」「差異を要因に分解する」「着地を予測する」の 4 点です。次の 6 本柱を、費用分析で差がつく順に並べました。

1. 費用データの整形（Power Query）：複数ベンダーの実績を結合し、月別予算表をアンピボット、費目マスタをマージして表記ゆれを統合する。
2. 正しい単価・比率はメジャーで：平均単価＝Σ費用/Σ工数、消化率＝Σ実績/Σ予算。行ごとの値を単純平均する「平均の平均」を避ける。
3. 費用差異の要因分解（ブリッジ）：予算と実績の差を、単価差（価格）・工数差（数量）・ミックス（費目/ベンダー構成）に分ける。
4. バーンレート＆完成時総コスト予測（EAC/ETC）：月次消化のペースから着地額を見通し、予算超過を早期に警告する。
5. Top-N ベンダー/費目＋寄与度：上位を残して「その他」に集約し、前月差・前年差をどの費目・ベンダーが動かしたかで説明する。
6. 費用ダッシュボード化＆ QA：安定参照とスライサー連動でインタラクティブに点検し、税区分混在や二重計上などの誤りを定型チェックで潰す。

これらは ECL（予想信用損失）システム導入 PMO の費用管理で、「いくら掛かったか」の報告を「なぜそうなったか・このままどこへ着地するか」の説明へ引き上げます。`;

// 1. 費用データの整形（Power Query）。
export const COST_PIVOT_PRO_POWERQUERY_MD = `### 1. 費用データの整形（Power Query）

複数ベンダーの実績は、Excel・CSV がフォーマットもバラバラで届きます。集計の前に、再現可能な前処理（Power Query）で 1 つの整然テーブルにまとめます。元データに手で列を足すのと違い、毎月「更新」ボタン 1 つで同じ整形を再現できます。

- 結合（append）：A 社・B 社・社内分の実績ファイルを縦に積み、1 つの実績テーブルにします。
- アンピボット：横持ちの月別予算表（列が 4 月・5 月・6 月…）を「発生月／予算額」の縦持ちに変換します。ピボットの値に置けるのは縦持ちだけです。
- マージ（merge）：費目マスタを結合し、「外注」「業務委託」「委託費」などの表記ゆれを正式な費目名に統合します。

あわせて、費用分析ならではの正規化を前処理に含めます。

- 税抜／税込：いずれかに統一します（混在は二重計上・差異の温床です）。税込で届く実績は税抜へ揃えます。
- 按分：共通費（ライセンス・PMO 費など）を案件・費目へ按分ルールで割り付けます。
- 計上月：請求日でなく発生月（役務提供月）に正規化し、月ズレを防ぎます。`;

// 2. 正しい単価・比率はメジャーで（平均の平均回避）。
export const COST_PIVOT_PRO_MEASURE_MD = `### 2. 正しい単価・比率はメジャーで（平均の平均を避ける）

単価や消化率は、行ごとに計算した値を単純平均すると誤ります。各課題の単価（費用/工数）を平均しても、全体の平均単価にはなりません（小さな案件に引っ張られます）。正しくは「合計どうしの割り算」です。

- 平均単価＝Σ費用 ÷ Σ工数（行単価の平均ではありません）
- 消化率＝Σ実績 ÷ Σ予算

これをデータモデルのメジャー（DAX）で定義すると、行・列・スライサーをどう変えても常に「合計 ÷ 合計」で再計算されます。

- \`平均単価 = DIVIDE(SUM(実績[費用]), SUM(実績[工数]))\`
- \`消化率 = DIVIDE(SUM(実績[費用]), SUM(予算[予算額]))\`

集計フィールドでも「費用の合計／工数の合計」は正しく出せますが、メジャーなら複数表をまたいだ消化率まで一貫して扱えます。`;

// 3. 費用差異の要因分解（ブリッジ）。
export const COST_PIVOT_PRO_BRIDGE_MD = `### 3. 費用差異の要因分解（ブリッジ）

「予算より 600 千円超えた」だけでは打ち手になりません。コンサルの定番は、差異を 3 つの要因に分解することです（price / volume / mix）。

- 単価差（価格）：ベンダー単価が想定より高い／低いことによる差。＝（実績単価 − 予算単価）× 実績工数。
- 工数差（数量）：投入工数が計画とずれたことによる差。＝（実績工数 − 予算工数）× 予算単価。
- ミックス：費目・ベンダーの構成比が変わったことによる差（高単価ベンダーの比率が増えた等）。

予算を起点に、価格・数量・ミックスの増減を積み上げて実績に着地させるウォーターフォール（ブリッジ）にすると、超過の「中身」が一目で説明できます。打ち手も要因ごとに変わります（価格＝単価交渉、数量＝スコープ管理、ミックス＝発注配分）。`;

// 4. バーンレート＆完成時総コスト予測（EAC/ETC）。
export const COST_PIVOT_PRO_EAC_MD = `### 4. バーンレート＆完成時総コスト予測（EAC／ETC）

費用は「今いくら」より「このままいくらで終わるか」が重要です。月次の消化額（バーンレート）から先を見通します。

- バーンレート：直近数か月の平均月次消化額。月あたりどれだけ費用が出ているかのペースです。
- ETC（残作業コスト）：残工程の見積。バーンレート × 残月数、または残タスクの積み上げで出します。
- EAC（完成時総コスト）＝実績累計 ＋ ETC。プロジェクト終了時の着地見込み額です。

EAC が予算（BAC）を超えるなら、終わる前に超過が分かります。累計費用の実績（折れ線）に、予測（点線）・予算ライン・EAC 着地点を重ねると、「いつ予算ラインを越えるか」「最終的にいくら超過するか」が読めます。早期警告として毎月更新します。`;

// 5. Top-N ベンダー/費目＋寄与度＋構成比ミックス。
export const COST_PIVOT_PRO_TOPN_MD = `### 5. Top-N ベンダー/費目＋寄与度＋構成比ミックス

ベンダーや費目が多いと、全部を並べた表は読めません。費用上位だけ残し、残りを「その他」に集約します。

- Top-N：費用降順で上位 N 件（例：上位 5 ベンダー）を表示し、残りを「その他」1 行にまとめます。値フィルター（上位 N）で抽出できます。
- 寄与度：前月差・前年差を、どの費目・ベンダーが押し上げた／押し下げたかに分解します。各項目の「当期 − 前期」を増減の大きい順に並べると、総増減への寄与が分かります。
- ミックス（構成比）：金額だけでなく構成比の変化も見ます。総額は横ばいでも、高単価ベンダーの比率が上がっていれば将来の超過リスクです。

「総額が前月から +500 千円。うち A 社が +700、ライセンスが −200」のように、増減を寄与度ウォーターフォールで説明できると、報告がそのまま打ち手の議論になります。`;

// 6. 費用ダッシュボード化＆ QA。
export const COST_PIVOT_PRO_DASHBOARD_QA_MD = `### 6. 費用ダッシュボード化＆ QA

最後に、点検と報告を回せる形に仕上げ、数字の正しさを定型チェックで担保します。

- 安定参照：月次サマリや対予算表のセルは GETPIVOTDATA で参照すると、ピボットの行・列が増減しても参照が壊れません。報告テンプレートに安定して数字を流し込めます。
- スライサー連動：1 つのスライサー（案件・フェーズ・ベンダー）を複数のピボット／ピボットグラフに接続すると、ボタン 1 つで表もグラフも同時に絞り込めます。インタラクティブに点検できます。
- 報告資料化：ピボットグラフをそのまま報告デックに貼れます。スライサーで切り口を変えながら同じ画面で説明できます。

報告前に通す費用 QA チェックリスト（よくある誤りの定番）：

- 税区分の混在：税抜と税込が混ざっていないか。
- 二重計上：同じ請求を実績とマスタの両方で数えていないか。
- 計上月のズレ：請求日でなく発生月で計上しているか。
- 按分漏れ：共通費（ライセンス・PMO 費）が按分されず宙に浮いていないか。
- 更新忘れ：元データを変えた後、ピボットを更新したか。
- 総計突合：ピボットの総計が、元データの合計（SUM）と一致するか。`;
